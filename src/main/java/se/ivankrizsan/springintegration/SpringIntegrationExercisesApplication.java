package se.ivankrizsan.springintegration;

import org.springframework.boot.SpringApplication;
import org.springframework.boot.autoconfigure.SpringBootApplication;

/**
 * Main application class.
 */
@SpringBootApplication
public class SpringIntegrationExercisesApplication {

    /**
     * Application main entry method.
     *
     * @param inArgs Command-line arguments.
     */
    public static void main(final String[] inArgs) {
        SpringApplication.run(SpringIntegrationExercisesApplication.class, inArgs);
    }
}
